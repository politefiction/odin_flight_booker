<!DOCTYPE html>
<html>
  <head>
    <title>Odin Flight Booker | <%= content_for?(:title) ? yield(:title) : 'A More Round-About Way to Book Your Flights' %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p id="home"><%= link_to "Home", root_url %></p>
    <% flash.each do |key, value| %>
      <%= value %>
    <% end %>
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>
